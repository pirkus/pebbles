# Pebbles Multi-Tenant Configuration Example
# Copy this file to .env and update with your values

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/pebbles

# HTTP Server Configuration
PORT=8081

# SQS Configuration
# Set to "true" to enable SQS consumer
SQS_ENABLED=false
# Full URL of your SQS queue
SQS_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789/pebbles-progress
# AWS Region for SQS
AWS_REGION=us-east-1
# AWS Credentials (can also use IAM roles)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# Kafka Configuration
# Set to "true" to enable Kafka consumer
KAFKA_ENABLED=false
# Comma-separated list of Kafka brokers
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
# Topic name for progress updates
KAFKA_TOPIC_NAME=progress-updates
# Consumer group ID
KAFKA_GROUP_ID=pebbles-consumer

# Example multi-environment setup:
# Development: Both consumers disabled, local MongoDB
# Staging: SQS enabled, MongoDB Atlas
# Production: Both SQS and Kafka enabled, MongoDB Atlas cluster